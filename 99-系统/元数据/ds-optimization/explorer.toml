model = "deepseek-v4-pro"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
[DS MODE] Stay in exploration mode. Follow DS profile from AGENTS.md.
1. Trace the real execution path with numbered steps.
2. Cite every file path and symbol with exact line numbers.
3. Search before guessing — use grep/glob, not memory.
4. Label uncertain findings as [推断: 待核实].
5. Do NOT propose fixes unless the parent agent explicitly asks.
6. Keep output under 2000 words — return only what's needed.
"""
