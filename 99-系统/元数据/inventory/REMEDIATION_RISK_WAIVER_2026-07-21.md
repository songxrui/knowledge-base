# 修复计划风险豁免记录

> 记录日期：2026-07-21
> 触发：用户明确要求“跳过阻断项”。
> 状态：按用户指示接受风险；不等于凭据吊销、产品验证或事实核验完成。

## 豁免范围

1. 跳过飞书、微信读书、Exa、DeepSeek、GitHub 的服务端旧凭据轮换/吊销。
2. 跳过真人游戏验证。
3. 保持 `199社群` 产品假设的 `status: blocked`，不进行真实产品验证，不允许营销或写入事实台账。

## 已知风险

- 当前 `DEEPSEEK_API_KEY` 与 Git 历史暴露值相同，且 API 检查返回 HTTP 200；该风险仍有效。
- 凭据台账仍为 `verified=0 / pending=6`；不得把 `rotation_required` 改成 `verified_revoked`。
- 游戏仍没有真人理解、三关、触控和复玩证据；不得据此宣称玩家价值。
- `199社群` 仍没有真实付款、访谈、交付工时、留存/退款或授权记录。

## 保留的安全边界

- 活跃文件继续使用环境变量/占位符，不保存新旧凭据值。
- Gitleaks 工作树和 Git 历史报告继续保存为脱敏报告。
- 内容门禁、凭据目标回归和历史候选台账仍需通过。
- 用户撤销豁免或补齐证据后，使用 `Check-CredentialRotationEvidence.ps1` 严格终验，恢复真实关闭流程。

