# 缓冲区生命周期审计

> 审计日期：2026-07-15。覆盖全库 20 个 `00-缓冲区`、519 个现存文件。台账记录复核状态；已执行的删除另有哈希清单。

## 基线

| 状态 | 文件数 | 处理要求 |
|---|---:|---|
| 超过 30 天 | 314 | 2026-07-22 前决定提升、归档或删除 |
| 15-30 天 | 3 | 2026-07-29 前复核 |
| 14 天以内 | 185 | 2026-08-14 前复核 |
| 治理 README | 17 | 保留 |

20 个缓冲区中有 5 个缺少 README。最大的两个缓冲区现有 303 个文件，占全部缓冲文件约 58%；仍应优先治理。

## 最大风险区

| 缓冲区 | 文件 | 超过 30 天 | 当前决定 |
|---|---:|---:|---|
| `05-内容生产/内容体系/00-缓冲区/` | 137 | 134 | 已删除 233 个失效装配稿；继续复核 legacy 内容 |
| `08-媒体与产品/媒体/wechat_2026-06-07/00-缓冲区/` | 166 | 155 | 已删除 18 个派生中间件；继续复核长文和草稿 |

## 台账

- `buffer-summary.csv`：每个缓冲区的规模、年龄和 README 状态。
- `buffer-file-disposition.csv`：519 个现存文件的 owner、年龄、复核期限和处置决定。
- `content-system-buffer-hash-audit.csv`：清理前 370 个文件与正式层的精确哈希对照。
- `content-system-buffer-quality-audit.csv`：365 个 Markdown 的断链、占位符、模板和乱码检查。
- `deleted-generated-assemblies-manifest.csv`：已删除 233 个生成装配稿的路径、大小、SHA-256 和理由。
- `corrupted-legacy-recovery-candidates.csv`：10 篇乱码稿的同名恢复来源检查；当前均未找到同名副本，因此继续保留。
- `wechat-buffer-audit.csv`：清理前 184 个微信缓冲文件的哈希、来源、发布和过程分类。
- `wechat-draft-name-matches.csv`：50 篇草稿与正式层的同名匹配；当前均无同名正式稿。
- `deleted-wechat-system-process-manifest.csv`：已删除 18 个 `_system` 派生中间件的路径、SHA-256 和理由。

所有非治理文件的 `disposition` 默认为 `pending`。只有人工确认用途和目标位置后，才能改为 `promote`、`archive` 或 `delete`。

## 已执行清理

233 个 `选题装配/` 文件满足一致的删除证据：233/233 含失效 wikilink，232/233 含占位内容，228/233 含重复通用模板；全部 1,179 条链接中有 1,178 条无法解析。删除前已固化路径和 SHA-256。131 篇 legacy Markdown 未随之删除，其中 10 篇仅标记为乱码损坏候选。

微信缓冲区 184 个文件中没有精确正式副本，也没有发布链接或发布日期证据。已删除 18 个 `_system` 派生中间件：其上游 W 文稿仍保留，且中间件包含未经核实的个人数据和文件名/正文类型错配。112 个 `_dbs` 文件（111 个文章文件、1 份报告）与 50 篇草稿继续保留待审。

## 执行规则

1. 优先处理超过 30 天且可重建的过程文件。
2. 提升文件时必须填写 `promote_to`，并确认正式层不存在更权威版本。
3. 删除前保留数量、路径和理由记录；不得仅凭“低质量”批量删除。
4. 每周重新生成台账；30 天以上无 owner 文件必须归档或删除。
