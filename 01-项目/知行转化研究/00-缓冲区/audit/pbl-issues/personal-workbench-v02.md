# project-build-loop：个人工作台 0.2 问题记录

## P0-00

已将多人招募门槛移除，建立单操作者 14 天数字→纸面双条件协议。真实试用尚未发生；顺序效应和熟悉任务的学习效应保留为限制。

## P0-01

`renderWorkspace()` 只保留一个可见工作区；刷新从业务状态推导当前任务，恢复态独立显示。主操作由当前表单或“记录观察”下一步卡承担。

## P0-02

合同分三段，当前阶段 Enter 只推进当前阶段；字段错误关联到具体控件并聚焦可见字段，风险冲突保留输入。

## P0-03

移动端账本使用纵向记录列表；当前版本和全部历史显式切换，统计口径不因列表筛选改变。

## P0-04

保留深绿色主身份，增加系统字体、中性表面、深色、高对比度、减少动画和透明度降级；正文不使用全屏毛玻璃。

## P0-05

数据菜单隔离导入、导出和清空；无效导入、恢复数据、跨标签冲突、清空取消和退出取消均保留原状态；键盘路径覆盖 Tab、Shift+Tab、Enter、Space、Escape。
